"""Framework to validate SEAS5 anomaly adjustments.

Produces the figures discussed for paper Section 2.X. Each module handles
one figure/metric family; `datasets.py` is the shared data-loading layer
so plotting code stays small.
"""
